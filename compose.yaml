services:
  workspace:
    build:
      context: .
      dockerfile: Dockerfile
      target: workspace
    volumes:
      - .:/workspace
      - node_modules:/workspace/node_modules
    init: true
    tty: true # for devcontainer

volumes:
  node_modules:
